interface:
  display_name: "Elev CRM"
  short_description: "Repo-specific patterns and workflows for Elev CRM"
  default_prompt: "Use the Elev CRM repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true